enum DownloadStatus {
  queued,
  downloading,
  paused,
  completed,
  failed,
  cancelled,
}

enum DownloadCategory {
  all,
  documents,
  images,
  videos,
  audio,
  compressed,
  programs,
  other,
}

enum SortOrder {
  dateAdded,
  name,
  size,
}

